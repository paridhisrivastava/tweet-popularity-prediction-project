This folder all the R scripts which build a classifier for Logistic Regression
To run the classifier, following libraries should be installed
1> caret
2> leaps
3> nnet
To run the files
1> Current directory should be set up using setwd() function which has testing and training data
2> All the files in this folder can be run and output will be written a different CSV files